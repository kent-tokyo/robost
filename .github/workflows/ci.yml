name: CI

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-test:
    name: build + test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libglib2.0-dev \
            libgtk-3-dev \
            libssl-dev \
            clang \
            libtesseract-dev \
            libleptonica-dev \
            libxdo-dev \
            libdbus-1-dev \
            libsecret-1-dev \
            libxkbcommon-dev \
            libasound2-dev \
            libfontconfig1-dev \
            libpipewire-0.3-dev \
            libspa-0.2-dev \
            libgbm-dev \
            xvfb

      - name: Build workspace
        run: cargo build --workspace

      - name: Run tests
        run: |
          if [ "$RUNNER_OS" = "Linux" ]; then
            xvfb-run -a cargo test --workspace
          else
            cargo test --workspace
          fi
        shell: bash

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Fmt check
        if: runner.os == 'Linux'
        run: cargo fmt --all --check

  validate-embed-editor:
    name: Validate embed-editor feature
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ubuntu-embed-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ubuntu-embed-

      - name: Install system dependencies
        run: |
          sudo apt-get update -q
          sudo apt-get install -y \
            pkg-config libglib2.0-dev libgtk-3-dev libssl-dev clang \
            libxdo-dev libdbus-1-dev libsecret-1-dev libxkbcommon-dev \
            libasound2-dev libfontconfig1-dev libpipewire-0.3-dev \
            libspa-0.2-dev libgbm-dev

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/web-editor/package-lock.json

      - name: Build web editor
        working-directory: apps/web-editor
        run: npm ci && npm run build

      - name: Build robost-cli with embed-editor
        run: cargo build -p robost-cli --features embed-editor
