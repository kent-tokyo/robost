# Statically link the MSVC CRT so the Windows binary runs without
# the Visual C++ Redistributable being installed on the target machine.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
